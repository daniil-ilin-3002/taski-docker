[flake8]
# Не проверять код на соответствие стандартам W503 и F811
ignore =
    W503,
    F811,
    E501,
    I001
# Не проверять код в перечисленных директориях/файлах:
exclude =
    .git,
    tests/,
    */migrations/,
    backend/backend/,
    backend/manage.py,
    venv,
    .venv,
    **/venv/**,
    **/.venv/**,
    env,
    **/env/**,
    **/site-packages/**,
# Не проверять указанные файлы на соответствующим правилам:
per-file-ignores =
    backend/**/settings.py:E501